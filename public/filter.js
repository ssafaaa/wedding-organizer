
    // document.addEventListener('DOMContentLoaded', function() {
    //     // Menangani klik pada tombol filter
    //     const buttons = document.querySelectorAll('.filter-tope-group button');

    //     buttons.forEach(button => {
    //         button.addEventListener('click', function() {
    //             // Mengambil class filter dari data-filter
    //             const filterClass = this.getAttribute('data-filter');

    //             // Sembunyikan semua kategori
    //             document.querySelectorAll('.isotope-grid').forEach(item => {
    //                 item.style.display = 'none';
    //             });

    //             // Tampilkan kategori yang dipilih
    //             const selectedItems = document.querySelectorAll(filterClass);
    //             selectedItems.forEach(item => {
    //                 item.style.display = 'block';
    //             });

    //             // Menandai tombol yang aktif
    //             buttons.forEach(btn => {
    //                 btn.classList.remove('how-active1');
    //             });
    //             this.classList.add('how-active1');
    //         });
    //     });
    // });

